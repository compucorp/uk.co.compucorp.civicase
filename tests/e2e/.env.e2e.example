# CiviCRM site under test
CIVICRM_BASE_URL=http://compuclient-87f26a.localhost:8080

# Admin UI login (Drupal user)
CIVICRM_ADMIN_USER=compuco_admin
CIVICRM_ADMIN_PASS=compuco_admin

# API credentials (used for setup/teardown via API3/API4 REST)
CIVICRM_API_KEY=your_admin_contact_api_key
CIVICRM_SITE_KEY=your_site_key

# Basic auth (only if the site is behind HTTP basic auth, e.g. cc-test)
# BASIC_AUTH_USER=COMPUCORP
# BASIC_AUTH_PASS=
